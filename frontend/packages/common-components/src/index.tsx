import locales from '@/locales';

import Components from './components';
import Hooks from '@/hooks';
import Request from '@/request';
import Utils from '@/utils';

export { locales, Components, Hooks, Request, Utils };

const Common = {
	locales,
	Components,
	Hooks,
	Request,
	Utils,
};
export default Common;
